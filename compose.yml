services:
  ynym-portal:
    build: .
    image: ynym-portal
    container_name: ynym-portal
    hostname: ynym-portal
    restart: always
    networks:
      - ynym-portal-network
      - nginx-proxy-network

networks:
  ynym-portal-network:
    name: ynym-portal-network
    external: true
  nginx-proxy-network:
    name: nginx-proxy-network
    external: true
